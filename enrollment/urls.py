from django.urls import path
from . import views # Make sure you have a views.py in this app

urlpatterns = [
    # Add your paths here. For now, we can leave it empty or add a placeholder
    # path('', views.assignment_list, name='assignment_list'),
]